import { MentionsLegalesModel } from './mentions-legales.model';

describe('MentionsLegalesModel', () => {
  it('should create an instance', () => {
    expect(new MentionsLegalesModel()).toBeTruthy();
  });
});
