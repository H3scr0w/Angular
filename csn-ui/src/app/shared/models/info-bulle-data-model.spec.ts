import { InfoBulleDataModel } from './info-bulle-data-model';

describe('InfoBulleDataModel', () => {
  it('should create an instance', () => {
    expect(new InfoBulleDataModel()).toBeTruthy();
  });
});
