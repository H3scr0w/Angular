import { RadioInputWarnMsgModel } from './radio-input-warn-msg.model';

describe('RadioInputWarmingMsgModel', () => {
  it('should create an instance', () => {
    expect(new RadioInputWarnMsgModel()).toBeTruthy();
  });
});
